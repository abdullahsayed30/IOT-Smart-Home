spring:
  profiles:
    active: dev

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: 'true'
        ddl-auto: create-drop
    generate-ddl: true
#    show-sql: true


server:
  error:
    include-message: always